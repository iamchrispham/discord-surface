{
  "name": "discord-surface",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "bin": {
    "discord-surface": "src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "npm run build && npm run typecheck && node --test --test-concurrency=1 test/topic.test.js test/surface.test.js test/native-transcript.test.js test/direct-post.test.js",
    "start": "node src/cli.js start"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
