# Final roadmap stress test

Source: authenticated Pro conversation https://chatgpt.com/c/6a9cf570-ccc0-83e8-86f6-f60e7ff5c05a. Visible completed response, subject SPARK-ROADMAP-FINAL. Advisory review of supplied evidence.

SPARK-ROADMAP-FINAL

Local mechanics below rely on your supplied verification, not repository inspection. The roadmap remains minimal, with five contract corrections inside S1–S5—not additional review stages.

1. REINFORCE — Outside-session publishing is the correct core; exclusion needs an end-to-end proof.

Artifact-only publishing can meet the primary objective, provided the required contextual sources exist. Spark must demonstrate incremental interpretive value, not receive credit for deterministic context savings.

Minimum correction: Identify automatic publications durably and test exclusion from immediate intake, Monitor, history, backfill, and post-reload reconstruction. No immediate queue submission is insufficient. Define reply-to-summary handling: the unchanged human message reaches its native owner, with a bounded reference to the particular artifact/evidence needed for that requested interaction—not neighboring automatic history. This preserves usable follow-ups without routine reinjection.

2. PARTIAL — S1 names source families, but still needs field-level producer contracts.

Beacons, queues and lane records do not, by themselves, establish an independent source for owed, intent and next. The discord-board.py limitation makes this a substantive dependency, not documentation cleanup.

Minimum correction: For each pilot channel, identify the concrete producer/path/event, revision, owner/generation and source timestamp for each required field. Distinguish empty, missing, stale and explicitly cleared. Fresh collection must not rejuvenate old evidence; disappearance must not imply completion. Bind progress-ladder transitions to identifiable evidence, never native turn endings.

Keep this a small mapping in existing code/tests. Missing owed-item sources block the complete contextual outcome.

3. PARTIAL — Exact native recognition is still an unproved capability dependency.

The supplied mechanics establish submitted custody, not semantic acknowledgment. “Actual owner emits acknowledgment” therefore needs an emission contract, not merely a reaction renderer.

Minimum correction: Establish each provider’s real normal-processing emission path, attributed through native reply custody and matched to the original message ID and delivery generation. Reject mismatched or superseded acknowledgments; duplicates must be harmless. Discord readback proves the reaction appeared, not that its underlying recognition evidence was legitimate.

No acknowledgment-only turn or submission-based substitute. An unsupported provider leaves S2 incomplete; inbox-only behavior is an honest partial result, not completion of the authorized acknowledgment goal.

4. PARTIAL — Cadence needs explicit continuous-change, failure and restart behavior.

The proposed defaults are reasonable hypotheses. Their corner cases need bounded semantics, not another scheduler.

Minimum correction: Make coalescing fixed-window or maximum-wait bounded so continuous events cannot indefinitely postpone publication. Recover the newest pending snapshot and remaining cooldown after reload. Separate failed-attempt retry/backoff from the last-success timestamp. Include one scoped startup/rearm read and one-shot staleness expiry, so a silent source cannot remain apparently fresh forever.

Cover atomic replacement, same-content churn, cooldown replacement, failed/uncertain send, restart-before-flush and old-generation model results using existing Gateway recovery guarantees. Required alerts and acknowledgments must bypass both cadence and model waits.

5. PARTIAL — Optional interpretation must remain evidence-gated through completion.

S3 permits choosing a policy from evidence, but S5 presently sounds committed to demonstrating both Spark and a selected Luna setting. That creates pressure to activate a tier whether or not it earns its overhead.

Minimum correction: Admit model work only for meaningful, publication-eligible candidates—not every file event—with bounded packet size, concurrency and deadline. Define which existing-policy warning classes may wake the conductor, including evidence-based deduplication and rearming. Luna uncertainty alone must not trigger escalation.

S3 may legitimately select artifact-only, Spark without Luna, or both. S5’s model-specific proofs apply to activated roles; rejected roles must be reported explicitly, not represented as successful features.

Minimum installed-pilot evidence

Use one joined acceptance record per provider within S5:

Source revision → scoped snapshot/evidence IDs → publication outcome/message ID → verified exclusion from native input, including exercised reload/history/backfill paths.

Genuine normal-processing acknowledgment → matching custody evidence → reaction readback; then a human follow-up reaching the correct persistent owner. Verify successor resolution separately without restarting native sessions.

No-change silence, required-alert bypass, deterministic fallback on model failure/timeout, and stale-result rejection. Include meaning preservation and discrepancy checks only for activated interpretation tiers.

Record installed versus loaded heads, observed automation-attributable native turns and input/context growth, acknowledgment/escalation overhead, sidecar latency/input, corrections, and operator comprehension. Mark unavailable measurements unavailable—not zero. Count actual /cs requests during the scoped pilot, but do not convert their absence into a counterfactual savings percentage.

Biggest false-completion risk

Mistaking “the updates posted without an immediate native turn” for “the workflow avoids conductor-context growth.” Delayed history/backfill ingestion and frequent policy-labelled warning escalations can recreate the original cost while the visible demo looks successful. The completion evidence must cover those routes, not just Discord delivery and a quiet queue at posting time.

Ready-to-build verdict

READY TO BUILD with the corrections above. Keep one writer and the three delivery slices: quiet acknowledgments, outside-context contextual snapshots, then evidence-gated interpretation. Resolve acknowledgment capability early; do not add another design panel.

Full completion requires independently sourced owed context, honest acknowledgment evidence for both providers, and installed-path exclusion proofs. Spark/Luna activation is conditional; the core outcome is not.
